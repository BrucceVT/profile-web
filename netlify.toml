# Netlify configuration for Bun-first project

[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.1.42"

# SPA fallback - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
